# Templates

These templates are copy-ready reference entry points for future projects.

Each template:

- imports only public APIs from `@weiyinblue/ui`
- assumes `@weiyinblue/ui/styles.css` is already loaded
- assumes the app is wrapped in `ThemeProvider`

Available templates:

- `portfolio`
- `campus-form`
- `admin-dashboard`
